# Module: windows-vm

Reusable Windows Server VM module for management and support scenarios.

Features:

- Windows Server 2022 deployment
- Standard static public IP for controlled RDP support access
- Credentials provided only via variables (no hardcoded secrets)
- Compatible with restricted RDP NSG rules using trusted source CIDRs
